def arithmetic_operations(a, b):
    sum_result = a + b
    difference = a - b
    product = a * b
    quotient = a / b if b != 0 else None
    return sum_result, difference, product, quotient

# Input two numbers
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

# Call the function and get multiple return values
sum_result, difference, product, quotient = arithmetic_operations(num1, num2)

# Display the results
print(f"The sum of {num1} and {num2} is {sum_result}")
print(f"The difference between {num1} and {num2} is {difference}")
print(f"The product of {num1} and {num2} is {product}")
print(f"The quotient of {num1} divided by {num2} is {quotient}")
111111111111111111111111111111111111
def greet(name, greeting="Hello"):
    return f"{greeting}, {name}!"


name = input("Enter your name: ")

custom_greeting = input("Enter a custom greeting message (press Enter to use the default greeting): ")


if custom_greeting:
    message = greet(name, custom_greeting)
else:
    message = greet(name)


print(message)
ZZZZZZZZZZZZZZZZZZZZZZZ
def string_length(s):
    length = 0
    for char in s:
        length += 1
    return length
user_string = input("Enter a string: ")
length_of_string = string_length(user_string)
print(f"The length of the string '{user_string}' is {length_of_string}.")

def is_substring_present(main_string, substring):
    for i in range(len(main_string) - len(substring) + 1):
        if main_string[i:i+len(substring)] == substring:
            return True
    return False
main_string = input("Enter the main string: ")
substring = input("Enter the substring to search for: ")
if is_substring_present(main_string, substring):
    print(f"The substring '{substring}' is present in the main string.")
else:
    print(f"The substring '{substring}' is not present in the main string.")

